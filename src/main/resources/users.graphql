schema {
    query: Query
}

type Query {
    chatInsert(senderId: String, receiverId: String, message: String, id: String) : Chat
    getChats(senderId: String, receiverId: String) : [Chat]
}

type Chat {
    senderId: String
    receiverId: String
    message: String
    id: String
}

